# tests/__init__.py Documentation

## Overview
This file serves as the package initializer for the `tests` directory. Its presence allows the directory to be treated as a Python package, facilitating test discovery by runners like `pytest`.

## Content Summary
The file currently contains a docstring labeling the directory as the "TubeHarvester Test Suite". It does not contain executable code, exported symbols, or configuration logic.
